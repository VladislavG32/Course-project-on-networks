# Курсовой проект: моделирование корпоративной сети (Cisco Packet Tracer)

Учебный проект по сетям: построение и настройка корпоративной сети в **Cisco Packet Tracer** с сегментацией **VLAN**, автоматической раздачей адресов **DHCP**, маршрутизацией **RIP + OSPF**, сервисами **DNS + HTTP** и трансляцией адресов **NAT**.

## 🎯 Цель проекта
Спроектировать и реализовать работоспособную сеть, в которой:
- приватная часть работает на **RIP v2**
- публичная (внешняя) часть работает на **OSPF**
- пограничный маршрутизатор **R2** выполняет **NAT** и обеспечивает доступность внутренних сервисов из публичной сети
- настроены **DHCP**, **DNS**, **HTTP**
- одна Ethernet-подсеть разделена на **4 VLAN** (по 2 ПК в каждом) + обеспечена **меж-VLAN маршрутизация**

## 🧩 Топология (общее описание)
Сеть разделена на две логические зоны:
- **Приватная сеть (RIP):** маршрутизаторы R1, R4, R6
- **Публичная сеть (OSPF):** маршрутизаторы R3, R5, R7
- **Пограничный маршрутизатор R2:** NAT + распространение маршрута по умолчанию во внутреннюю RIP-сеть

## 🧾 Адресация (вариант 17)
- Приватный диапазон: `10.199.0.0` … `10.203.0.0` (VLSM/бесклассовая адресация)
- Публичный диапазон: `130.228.47.0` … `130.228.51.0`
- Подсети **P2P**: маски **/30**
- Подсеть **№9** разделена на **4 VLAN** (каждая примерно /26)

> Точные адреса и IP устройств подписаны на схеме в Packet Tracer (заметки/labels).

## ✅ Реализовано по требованиям
- [x] Построена модель сети в Cisco Packet Tracer
- [x] Настроены **HTTP-серверы** (внутренний и внешний) + страницы
- [x] Планирование IPv4 с **VLSM**, резерв под P2P
- [x] Базовая настройка устройств (hostname, пароли, шлюзы)
- [x] Serial-линки с **разной пропускной способностью** (учтено в OSPF через `bandwidth`)
- [x] **DHCP** для Ethernet-подсетей (кроме VLAN-подсети) + исключения (SVI + HTTP)
- [x] **VLAN**: 4 VLAN по 2 ПК + меж-VLAN маршрутизация
- [x] Маршрутизация: **RIP внутри**, **OSPF снаружи**, default route на R2 и распространение в RIP
- [x] **DNS** в публичной сети + записи для доступа к внутреннему HTTP по имени
- [x] **NAT** на R2: динамический для клиентов + статический для публикации внутреннего HTTP

## 📦 Содержимое репозитория
- `топология8.0.pkt` — готовая схема сети (Packet Tracer)
- `Задание.pdf` — исходное ТЗ
- `кп 2.docx` — описание/пояснение по реализации

## ▶️ Как открыть и проверить (Packet Tracer)
1. Установи **Cisco Packet Tracer** (8.x).
2. Открой файл: `топология8.0.pkt`.
3. Перейди в **Realtime** (или Simulation для пошаговой проверки).

### Быстрые проверки
**DHCP**
- На ПК в обычных Ethernet-подсетях включи DHCP и проверь, что адрес выдан корректно.

**VLAN**
- Проверить ping между ПК в одной VLAN.
- Проверить ping между VLAN (меж-VLAN маршрутизация должна работать).

**Маршрутизация**
- На маршрутизаторах убедись, что есть маршруты RIP/OSPF и что во внутренней сети присутствует маршрут по умолчанию.

**NAT**
- Из публичной сети проверь доступ к внутреннему HTTP (через статический NAT/проброс).
- Проверь исходящий доступ внутренних узлов наружу (динамический NAT).

**DNS**
- Открой внутренний HTTP по доменному имени (DNS-запись).

## 🔎 Команды для демонстрации (на защите/проверке)
На маршрутизаторах:
- `show ip interface brief`
- `show ip route`
- `show ip protocols`
- `show ip ospf neighbor`
- `show ip nat translations`
- `show ip nat statistics`

На коммутаторах VLAN:
- `show vlan brief`
- `show interfaces trunk`

## 📌 Что я прокачал этим проектом
- VLAN / trunk / меж-VLAN маршрутизация
- DHCP (пулы + исключения)
- RIP v2 и OSPF (включая стоимость через bandwidth)
- DNS / HTTP сервисы
- NAT (динамический + статический)
- Базовый troubleshooting по связности и маршрутизации

## 👤 Автор
Остапенко Владислав
